/**
 * Demo seeding (P1). Will:
 *   1. create the demo session with the fast timer preset,
 *   2. add the three demo questions with their answers,
 *   3. pre-warm agent caches for inputs that are fixed (generator, narrator),
 *   4. print the join code, dashboard URL, and teacher token.
 *
 * Until then this is a stub so `npm run seed:demo` exists.
 */
console.log('seed-demo: not implemented yet (P1). Use `npm run simulate` to create a session with the demo questions.')
process.exit(0)
